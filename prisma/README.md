
## Database Setup

```bash
# Format the schema
npm run db:format

# Generate Prisma Client
npm run db:generate

# Create migration (when ready)
npm run db:migrate

# Push schema to database (dev)
npm run db:push

# Open Prisma Studio
npm run db:studio
```
